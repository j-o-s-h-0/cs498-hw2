The us-central1 instance (A) had significantly lower latency than the europe-west1 instance (B). The average latency on A was 52.82 ms for /register and 81.9 ms for /list. The average latency on B was 331.0 ms for /register and 375.3 ms for /list. In the consistency test, there were 0 misses out of 100 iterations.

The difference in latency between the two instances is likely due to geographic distance between them and my computer. Since I am physically closer to the us-central1 region, requests to that server require fewer network hops and less overall travel time. Requests to europe-west1 have to travel farther and across continents, which increases the round-trip time and latency measurements.

Although Firestore queries are eventually consistent, the consistency tests I ran all showed 0 misses out of 100 attempts. This indicates that replication between regions occurs very quickly in practice. Even though eventual consistency allows for short propagation delays, the system replicated updates fast enough that I did not observe any visible lag in my experiment. This experiment may hint at the fact that while eventual consistency exists, the delay may be too small to notice in many real-world scenarios.