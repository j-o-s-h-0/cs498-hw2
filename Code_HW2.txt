For Section IV, I wrote a simple Python script (part4.py) using the Python requests library. The script sends 10 requests to /register and 10 requests to /list on each VM instance and measures the time before and after each request is sent/received to compute average latency. For the consistency test, it runs a loop where it registers a user on one instance and immediately calls /list on the other instance to count how many times the new user does not appear. The rest of the assignment follows the provided instructions and uses the example code given.